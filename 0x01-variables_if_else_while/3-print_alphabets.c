#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/** main - prints lower and uppercase alphabets
 * Return: always success
 */

int main(void)
{
	char a;

	for (a = "a"; a <= z; a++)
		purchar(a);
	for (a = "A"; a <= Z; A++)
		purchar(a);
		purchar('\n');
	
	return (0);
}
